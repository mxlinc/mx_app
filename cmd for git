# check current status
git status

# stage all changes
git add .

# commit with a message
git commit -m "
git push origin main


QB is gone
git push origin 
qb



# pull latest from remote (avoid conflicts)
git pull origin main

# push your local commits to remote
git push origin main






DONE -- git tag -a qb-v1 -m "Quiz builder MCQ"  

git tag -a before-big-change -m "safe point"
git push origin before-big-change


Read the finetune page /unit/assign-finetune thoroughly and add a detailed section to the architecture notes covering its current implementation.

Update the memory file to reflect what was changed.

